# ---------------------------------------------------------------------------
# Astro Blog Template — Environment Variables
# ---------------------------------------------------------------------------
# Required for connecting to the OpenYapper CMS backend.
#
# Quick start with the helper script:
#   ./start-preview.sh <site-slug> [port]
#   ./start-preview.sh john-doe 4321
#
# The script resolves the site UUID automatically. You only need to set
# CMS_API_URL and CMS_API_KEY below (or export them).
# ---------------------------------------------------------------------------

# Backend API base URL (include /api/v1 prefix)
CMS_API_URL=http://localhost:8000/api/v1

# API key with at least Read permission for the target site
CMS_API_KEY=

# Site UUID — set manually or let start-preview.sh resolve it from slug
CMS_SITE_ID=
